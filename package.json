{
  "name": "intervals-icu-cli",
  "version": "0.1.6",
  "description": "Bridge for intervals.icu API - athletic training data management",
  "main": "dist/index.js",
  "bin": {
    "intervals-icu": "bin/intervals-icu"
  },
  "type": "module",
  "scripts": {
    "build": "bun run scripts/build.ts",
    "prepublishOnly": "npm run build",
    "test": "bun test",
    "start": "bun run src/index.ts"
  },
  "dependencies": {
    "commander": "^11.0.0",
    "dotenv": "^16.3.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.6",
    "typescript": "^5.0.0"
  }
}
